[data]
    # EDIT THIS: Set to "work" or "personal" for each machine
    machineType = "personal"
    
    # Email addresses (used automatically based on machineType)
    workEmail = "anstr@tdc.nuuway.dk"
    personalEmail = "a.stroem+github@pm.me"
    
    # Your name
    name = "Anders Strøm"
    
    # Computed values (don't edit these)
    isWork = false
    isPersonal = true

[git]
    autoCommit = false
    autoPush = false

[diff]
    command = "code"
    args = ["--wait", "--diff"]

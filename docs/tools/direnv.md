# direnv

## Syntax
`direnv <command>`

## Core commands
- `direnv allow`
- `direnv deny`
- `direnv status`

## Typical flow
1. Add `.envrc` to project.
2. Run `direnv allow`.
3. Enter directory and variables auto-load.

# zellij

## Syntax
`zellij [command]`

## Common commands
- `zellij` (start a new session)
- `zellij attach <name>`
- `zellij list-sessions`
- `zellij kill-session <name>`

## Notes
Opt-in, local-machine only (`installZellij` profile). tmux remains the
multiplexer used on remote/server machines.

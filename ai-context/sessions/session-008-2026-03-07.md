# Session 008 — 2026-03-07

## Summary

Long homelab session covering NAS setup completion, app migration from Pi to NAS, reverse proxy overhaul, cert automation, and documentation.

---

## Tasks Completed

### NFS Fix
- `/Volumes/Working` mount point was missing — created it, NFS mount worked immediately

### App Migration: Pi → NAS
- Migrated `cgpm`, `finance-tracker`, and `shared-postgres` from Pi (microSD) to NAS (ZFS-backed)
- Repos cloned to `~/ops/` on NAS; persistent data on `/mnt/tank/apps/`
- Changed bind mounts from relative `./data` paths to absolute ZFS dataset paths
- Removed `shared_proxy_net` from all app compose files (nginx no longer on same host)
- Exposed ports on NAS containers for Pi nginx to reach

### Pi Reverse Proxy Overhaul
- Added nginx vhosts for: Nextcloud, TrueNAS UI (`nas.home.andersstroem.me`), Netbox
- Updated cgpm and finance upstreams from container names to `10.0.10.10:<port>`
- Renamed `docker-compose.https.yml` → `docker-compose.override.yml` (HTTPS now default)
- Removed `shared_proxy_net` from nginx compose (orphaned)
- Fixed Pi-hole conflict (lighttpd on port 80) — ran uninstall script, stopped `pihole-FTL` service

### Hostname Change
- Pi renamed from `pi-utility` to `pi-01` via `hostnamectl` + `/etc/hosts` fix

### TLS Cert Automation
- Certbot runs as Docker container with Cloudflare DNS plugin
- Created `certbot/renew.sh` renewal script (committed to docker-infra repo)
- Set up root cron: `0 3,15 * * * /opt/docker-infra/reverse-proxy/certbot/renew.sh`

### Netbox Migration: Mac → NAS
- Updated compose file: named volumes → ZFS bind mounts, port 8090 exposed
- `ALLOWED_HOSTS` now driven by `NETBOX_ALLOWED_HOSTS` env var
- Added `CSRF_TRUSTED_ORIGINS` to `.env` (required for Django behind reverse proxy)
- Added nginx vhost `netbox.home.andersstroem.me`

### ZFS Snapshots
- Configured snapshot schedules across all `tank` datasets
- Daily: 08:00–08:35 staggered 5 min apart
- Weekly: Sunday 10:00–10:20 staggered
- Trimmed `personal/media` weekly retention from 52w → 26w

### Tailscale ACLs
- Set up `tag:nas`, `tag:family`, `tag:client`
- `tag:family`: ports 30027 + 445 on `tag:nas`
- `tag:client`: port 30027 only

### Nextcloud
- Added `nextcloud.home.andersstroem.me` to trusted_domains
- Added `overwriteprotocol` and `trusted_proxies` for nginx proxy support
- Fixed PHP syntax error (missing comma in trusted_domains array)

---

## Files Modified

### Obsidian Vault
- `nas-01 Configuration.md` — Docker apps section, trusted_domains, Tailscale IP, snapshot schedule, removed done pending items
- `Homelab Naming Conventions.md` — snapshot schedule times, 1Password fill behaviour section
- `IP Address Plan.md` — updated cgpm/finance/netbox service entries
- `Deploying Apps on NAS.md` — architecture diagram, Netbox env, directory layout, Pi paths fixed
- `pi-01 Configuration.md` — **new** — full Pi reference doc
- `Managing TLS Certificates.md` — **new** — certbot from scratch + renewal
- `Tailscale Configuration.md` — **new** — ACL, tags, onboarding/removal
- `Onboarding a Family Member.md` — **new** — full onboarding process
- `Docker Compose Cheatsheet.md` — **new** — common flags and patterns

### Code Repos
- `docker-infra`: nginx vhosts (nextcloud, nas, netbox, cgpm, finance), removed shared_proxy_net, renamed https override, certbot renew.sh
- `color-grading-pm`: absolute bind mounts, exposed ports, removed shared_proxy_net
- `finance-tracker`: exposed ports, removed shared_proxy_net
- `nas-infra/netbox`: bind mounts, ALLOWED_HOSTS env var, port 8090

---

## Key Decisions

- Apps repo code lives in `~/ops/` on NAS (not `/opt` — TrueNAS root filesystem not guaranteed across upgrades)
- `docker-infra` lives in `/opt/docker-infra/` on Pi (consistent with existing layout)
- Certbot runs on-demand as Docker container, not persistent
- HTTPS is default for nginx (`docker-compose.override.yml` auto-merged)
- Per-service `.env` files on NAS (not repo-wide — no direnv on NAS)
- Two Tailscale tags (`tag:family`, `tag:client`) not three — friends treated as family

---

## Pending / Next Session

- Remaining 2 SSDs arrive → add second mirror vdev to `work` pool
- 10GbE switch arrives → repatch both ports, no IP changes needed
- Onboard family members (Nextcloud accounts, datasets, SMB shares)
- Set up Grafana + Prometheus monitoring (nas-infra repo)
- Install Ghostty terminfo on Pi: `infocmp | ssh asj@10.0.10.11 'tic -x -'`
- Migrate colorgrading projects from external SSD to `tank/colorlab/archive` (in progress)

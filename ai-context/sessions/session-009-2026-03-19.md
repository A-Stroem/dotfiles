# Session 009 — 2026-03-19

## Context
Home directory / multi-repo workspace (`/Users/andersstrom/dev`).

## Tasks Completed

### 1. Immich Implementation Plan (nas-infra)
- Reviewed and adjusted an Immich implementation plan for the actual environment
- Key adjustments: Ansible-managed deployment (not root), correct ZFS dataset paths (`tank/apps/immich/`), dataset ownership before first run, no nginx on NAS
- Created `infrastructure/nas-infra/docs/plans/2026-03-18-immich.md`

### 2. Immich Docker Compose Stack
- Created `infrastructure/nas-infra/immich/docker-compose.yml` and `.env.example`
- Stack: immich-server, immich-microservices, immich-postgres (pgvecto-rs), immich-redis, cloudflared
- ML disabled (`MACHINE_LEARNING_ENABLED=false`)
- No nginx — LAN/Tailscale on port 2283, external via Cloudflare Tunnel

### 3. Cloudflare Tunnel Setup
- Guided setup of Cloudflare Tunnel in Zero Trust dashboard
- Public hostname: `immich.andersstroem.me` → `http://immich-server:2283`
- Attempted Cloudflare Access (email allowlist) — confirmed incompatible with Immich mobile app even with `/api` bypass policy. Removed.

### 4. Immich v2.x Port Fix
- Discovered Immich v2.x listens on port 2283 internally (not 3001 as in v1.x)
- Fixed compose: `2283:3001` → `2283:2283`, healthcheck endpoint updated to `/api/server/ping`

### 5. ZFS Dataset Setup (on nas-01)
- Datasets: `tank/apps/immich/db` (64K), `tank/apps/immich/cache` (64K), `tank/apps/immich/library` (1M)
- Backup dataset: `tank/backups` (parent) + `tank/backups/immich` (5G quota)
- Dataset ownership set before first start (UID 999 for postgres, 1000 for immich)

### 6. Deployment
- Files deployed to `/mnt/tank/apps/ansible/services/immich/` on nas-01
- Stack running and healthy
- Mobile app working through Cloudflare Tunnel after removing Cloudflare Access

## Key Decisions
- No nginx on NAS — all NAS services accessed directly on port or via Cloudflare Tunnel
- Cloudflare Access is incompatible with Immich mobile app (blocks auth flow, OAuth callback URLs rejected)
- Storage template: `{{y}}/{{y}}-{{MM}}-{{dd}}/{{filename}}`
- Full-size image generation enabled (for HEIC/RAW), wide gamut enabled, prefer embedded preview off
- Admin account kept separate; personal user accounts created per family member

## Files Modified
- `infrastructure/nas-infra/immich/docker-compose.yml` (created)
- `infrastructure/nas-infra/immich/.env.example` (created)
- `infrastructure/nas-infra/docs/plans/2026-03-18-immich.md` (created)
- All committed and pushed to `github.com:A-Stroem/nas-infra.git`

## Pending / Follow-up
- SFTP container (`ix-sftp-sftp-1`) was showing `Exited (127)` — not addressed this session
- Backup cron for Immich PostgreSQL not yet deployed via Ansible
- Tailscale was fixed at start of session but not fully documented
- `provision_ansible_user.yml` against srv-01 still pending (from previous session)
- truenas_admin sudo/permissions issue noted but not fully resolved
